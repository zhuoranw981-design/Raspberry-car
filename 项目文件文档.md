# 小车避障云端AI系统 - 完整项目文件文档

## 项目概述

**项目名称**: 小车避障云端AI服务  
**项目类型**: 智能小车云端控制系统  
**开发语言**: Python 3.9+  
**主要框架**: FastAPI, MQTT  
**部署环境**: Linux/Windows  

---

## 目录结构

```
云端/
├── app.py                              # 主应用入口文件
├── config.py                           # 配置文件（需创建）
├── requirements.txt                    # Python依赖包列表（需创建）
├── services/                           # 业务服务模块目录
│   ├── decision_engine.py             # 决策引擎 - 智能避障核心
│   ├── mqtt_service.py                # MQTT服务 - 消息收发
│   └── data_processor.py              # 数据处理服务（需创建）
├── static/                            # 静态文件目录（需创建）
│   └── index.html                     # Web前端监控界面
├── logs/                              # 日志目录（需创建）
├── venv/                              # Python虚拟环境（自动生成）
├── 部署报告.md                         # 部署报告文档
└── 项目文件文档.md                     # 本文档
```

---

## 一、核心文件详解

### 1.1 主应用入口 - app.py

**文件路径**: `app.py`  
**文件大小**: ~8KB  
**功能描述**: 系统主入口，提供Web服务、API接口和WebSocket实时通信

#### 1.1.1 导入模块

```python
import asyncio
from fastapi import FastAPI, WebSocket, Request
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from fastapi.responses import HTMLResponse, JSONResponse
from config import Config
from services.data_processor import DataProcessor
from services.decision_engine import DecisionEngine
from services.mqtt_service import MQTTService
from datetime import datetime
from websockets.exceptions import ConnectionClosed
```

**模块说明**:
- `asyncio`: 异步编程支持
- `FastAPI`: Web框架，提供REST API和WebSocket
- `CORS`: 跨域资源共享中间件
- `Config`: 配置类，管理系统配置参数
- `DataProcessor`: 数据处理服务
- `DecisionEngine`: 决策引擎，智能避障核心
- `MQTTService`: MQTT消息服务

#### 1.1.2 全局变量

```python
app = FastAPI(title="小车避障云端AI服务")

config = Config()
data_processor = DataProcessor()
decision_engine = DecisionEngine(config)
mqtt_service = MQTTService(config, data_processor, decision_engine)

active_connections = set()
latest_camera_frame = None
latest_manual_command = None
mqtt_initialized = False

CONTROL_PASSWORD = "123456"
authenticated_clients = set()
```

**变量说明**:
- `app`: FastAPI应用实例
- `config`: 配置对象
- `data_processor`: 数据处理器实例
- `decision_engine`: 决策引擎实例
- `mqtt_service`: MQTT服务实例
- `active_connections`: 活跃的WebSocket连接集合
- `latest_camera_frame`: 最新摄像头帧
- `latest_manual_command`: 最新手动命令
- `CONTROL_PASSWORD`: 控制密码（默认123456）
- `authenticated_clients`: 已认证的客户端IP集合

#### 1.1.3 中间件配置

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**功能**: 允许跨域请求，支持所有来源、方法和请求头

#### 1.1.4 静态文件挂载

```python
app.mount("/static", StaticFiles(directory="static"), name="static")
```

**功能**: 挂载静态文件目录，提供前端资源访问

#### 1.1.5 生命周期事件

##### 启动事件

```python
@app.on_event("startup")
async def startup_event():
    global mqtt_initialized
    mqtt_service.start()
    mqtt_service.set_event_loop(asyncio.get_running_loop())
    mqtt_service.on_message_callback = broadcast_message
    mqtt_initialized = True
    print("🚀 小车避障云端AI服务启动成功")
    print(f"🔐 控制密码已设置")
```

**功能**: 
- 启动MQTT服务
- 设置事件循环
- 注册消息回调
- 初始化完成标志

##### 关闭事件

```python
@app.on_event("shutdown")
async def shutdown_event():
    mqtt_service.stop()
```

**功能**: 优雅关闭MQTT服务

#### 1.1.6 路由接口

##### GET / - 主页

```python
@app.get("/", response_class=HTMLResponse)
async def root(request: Request):
    with open("static/index.html", "r", encoding="utf-8") as f:
        return HTMLResponse(content=f.read())
```

**功能**: 返回Web前端监控界面

##### GET /health - 健康检查

```python
@app.get("/health")
async def health_check():
    return {"status": "healthy", "mqtt_connected": mqtt_service.connected}
```

**功能**: 检查服务健康状态和MQTT连接状态

**返回示例**:
```json
{
    "status": "healthy",
    "mqtt_connected": true
}
```

##### GET /latest - 获取最新数据

```python
@app.get("/latest")
async def get_latest_data():
    return {
        "timestamp": datetime.now().isoformat(),
        **data_processor.get_latest_data(),
        "last_command": latest_manual_command or decision_engine.get_last_command(),
        "mode_info": decision_engine.get_mode_info(),
        "camera": latest_camera_frame
    }
```

**功能**: 获取最新的传感器数据、命令和模式信息

**返回示例**:
```json
{
    "timestamp": "2026-05-12T10:30:00.000Z",
    "radar": {"timestamp": "...", "data": [...]},
    "ultrasonic": {"timestamp": "...", "data": {...}},
    "pose": {"timestamp": "...", "data": {...}},
    "last_command": {"action": "forward", "speed": 20, ...},
    "mode_info": {"auto_mode": true, "auto_speed": 20, ...},
    "camera": "base64_encoded_image"
}
```

##### POST /api/login - 密码认证

```python
@app.post("/api/login")
async def login(request: Request):
    try:
        data = await request.json()
        password = data.get('password', '')
        client_ip = request.client.host
        
        if password == CONTROL_PASSWORD:
            authenticated_clients.add(client_ip)
            print(f"🔐 客户端 {client_ip} 已认证")
            return JSONResponse(content={
                "success": True,
                "message": "认证成功"
            })
        else:
            print(f"❌ 客户端 {client_ip} 密码错误")
            return JSONResponse(content={
                "success": False,
                "message": "密码错误"
            })
    except Exception as e:
        return JSONResponse(content={"success": False, "error": str(e)}, status_code=500)
```

**功能**: 客户端密码认证

**请求参数**:
```json
{
    "password": "123456"
}
```

**返回示例**:
```json
{
    "success": true,
    "message": "认证成功"
}
```

##### POST /api/command - 发送控制命令

```python
@app.post("/api/command")
async def send_command(request: Request):
    global latest_manual_command
    
    client_ip = request.client.host
    if client_ip not in authenticated_clients:
        return JSONResponse(content={
            "success": False,
            "message": "请先登录"
        }, status_code=401)
    
    try:
        cmd = await request.json()
        print(f"📥 收到API命令: {cmd}")
        
        if 'auto_mode' in cmd:
            auto_mode = cmd['auto_mode']
            speed = cmd.get('speed', 20)
            safe_distance = cmd.get('safe_distance', 50)
            warning_distance = cmd.get('warning_distance', 100)
            
            decision = decision_engine.set_auto_mode(auto_mode, speed, safe_distance, warning_distance)
            latest_manual_command = None
            
            if mqtt_service.connected:
                mqtt_service.publish_command(decision)
            
            return JSONResponse(content={
                "success": True,
                "message": f"已{'启用' if auto_mode else '关闭'}自动避障模式",
                "speed": decision['speed'],
                "auto_mode": auto_mode
            })
        
        action = cmd.get('action', 'stop')
        speed = cmd.get('speed', 20)
        
        latest_manual_command = {
            'action': action,
            'speed': speed,
            'timestamp': datetime.now().isoformat(),
            'reason': '手动控制'
        }
        
        if mqtt_service.connected:
            mqtt_service.publish_command(latest_manual_command)
            print(f"📤 发送手动命令: {action} {speed}")
        
        return JSONResponse(content={
            "success": True,
            "message": f"命令已发送: {action}",
            "action": action,
            "speed": speed
        })
    
    except Exception as e:
        print(f"❌ 命令处理失败: {e}")
        return JSONResponse(content={"success": False, "error": str(e)}, status_code=500)
```

**功能**: 接收手动控制命令或切换自动避障模式

**请求参数（手动控制）**:
```json
{
    "action": "forward",
    "speed": 30
}
```

**请求参数（自动模式）**:
```json
{
    "auto_mode": true,
    "speed": 20,
    "safe_distance": 50,
    "warning_distance": 100
}
```

**返回示例**:
```json
{
    "success": true,
    "message": "命令已发送: forward",
    "action": "forward",
    "speed": 30
}
```

##### GET /api/mode - 获取当前模式

```python
@app.get("/api/mode")
async def get_mode():
    return decision_engine.get_mode_info()
```

**功能**: 获取当前控制模式和参数

**返回示例**:
```json
{
    "auto_mode": true,
    "auto_speed": 20,
    "safe_distance": 50,
    "warning_distance": 100
}
```

#### 1.1.7 WebSocket端点

##### WebSocket /ws - 实时数据推送

```python
@app.websocket("/ws")
async def websocket_endpoint(websocket: WebSocket):
    await websocket.accept()
    active_connections.add(websocket)
    print(f"🔗 新连接，当前连接数: {len(active_connections)}")

    try:
        while True:
            mode_info = decision_engine.get_mode_info()
            
            if mode_info['auto_mode']:
                current_cmd = decision_engine.get_last_command()
            else:
                current_cmd = latest_manual_command or {
                    'action': 'stop',
                    'speed': 0,
                    'timestamp': datetime.now().isoformat(),
                    'reason': '等待命令'
                }
            
            latest_data = {
                "type": "sensor_data",
                "timestamp": datetime.now().isoformat(),
                **data_processor.get_latest_data(),
                "last_command": current_cmd,
                "mode_info": mode_info,
                "camera": latest_camera_frame
            }
            await websocket.send_json(latest_data)
            await asyncio.sleep(0.1)
            
    except ConnectionClosed:
        print("🔌 WebSocket连接正常关闭")
    except Exception as e:
        print(f"❌ WebSocket异常: {e}")
    finally:
        active_connections.discard(websocket)
        print(f"🔌 连接断开，当前连接数: {len(active_connections)}")
```

**功能**: 实时推送传感器数据和控制命令

**推送数据格式**:
```json
{
    "type": "sensor_data",
    "timestamp": "2026-05-12T10:30:00.000Z",
    "radar": {...},
    "ultrasonic": {...},
    "pose": {...},
    "last_command": {...},
    "mode_info": {...},
    "camera": "base64_image"
}
```

#### 1.1.8 辅助函数

##### broadcast_message - 广播消息

```python
async def broadcast_message(message):
    global latest_camera_frame
    
    if message.get('type') == 'camera':
        latest_camera_frame = message.get('data')
        return
    
    disconnected = []
    for connection in active_connections:
        try:
            await connection.send_json(message)
        except (ConnectionClosed, Exception):
            disconnected.append(connection)
    
    for conn in disconnected:
        active_connections.discard(conn)
```

**功能**: 向所有WebSocket连接广播消息

#### 1.1.9 主程序入口

```python
if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host=config.API_HOST, port=config.API_PORT)
```

**功能**: 启动Uvicorn服务器

---

### 1.2 决策引擎 - services/decision_engine.py

**文件路径**: `services/decision_engine.py`  
**文件大小**: ~6KB  
**功能描述**: 智能避障决策核心，基于传感器数据生成控制指令

#### 1.2.1 类定义

```python
class DecisionEngine:
    def __init__(self, config):
        self.config = config
        self.last_command = {
            'action': 'stop', 
            'speed': 0, 
            'timestamp': datetime.now().isoformat(),
            'reason': '初始状态'
        }
        self.auto_mode = False
        self.auto_speed = 20
        self.safe_distance = 50
        self.warning_distance = 100
        self.last_manual_time = None
```

**属性说明**:
- `config`: 配置对象
- `last_command`: 最后执行的命令
- `auto_mode`: 自动避障模式开关
- `auto_speed`: 自动模式速度
- `safe_distance`: 安全距离（厘米）
- `warning_distance`: 警告距离（厘米）
- `last_manual_time`: 最后手动控制时间

#### 1.2.2 方法详解

##### set_auto_mode - 设置自动避障模式

```python
def set_auto_mode(self, enabled, speed=None, safe_distance=None, warning_distance=None):
    self.auto_mode = enabled
    if speed is not None:
        self.auto_speed = speed
    if safe_distance is not None:
        self.safe_distance = safe_distance
    if warning_distance is not None:
        self.warning_distance = warning_distance
        
    if enabled:
        self.last_command = {
            'action': 'forward',
            'speed': self.auto_speed,
            'timestamp': datetime.now().isoformat(),
            'reason': '自动避障模式已启用'
        }
    else:
        self.last_command = {
            'action': 'stop',
            'speed': 0,
            'timestamp': datetime.now().isoformat(),
            'reason': '已切换为手动模式'
        }
    
    return self.last_command
```

**参数**:
- `enabled`: 是否启用自动模式（布尔值）
- `speed`: 自动模式速度（可选）
- `safe_distance`: 安全距离（可选）
- `warning_distance`: 警告距离（可选）

**返回**: 当前命令对象

##### set_manual_command - 设置手动命令

```python
def set_manual_command(self, action, speed):
    self.last_manual_time = datetime.now()
    self.last_command = {
        'action': action,
        'speed': speed,
        'timestamp': datetime.now().isoformat(),
        'reason': '手动控制'
    }
    return self.last_command
```

**参数**:
- `action`: 动作类型（forward, backward, left, right, stop）
- `speed`: 速度值（0-100）

**返回**: 当前命令对象

##### get_decision - 获取避障决策

```python
def get_decision(self, radar_obstacles, ultrasonic_data, pose_data):
    if not self.auto_mode:
        return self.last_command

    decision = {
        'action': 'forward',
        'speed': self.auto_speed,
        'reason': '安全',
        'timestamp': datetime.now().isoformat()
    }

    # 检测前方270度范围
    if radar_obstacles and len(radar_obstacles) > 0:
        front_obstacles = [
            obs for obs in radar_obstacles 
            if obs.get('angle', 0) >= -135 and obs.get('angle', 0) <= 135
        ]
        
        if front_obstacles:
            # 按角度分区检测
            left_obstacles = [obs for obs in front_obstacles if obs['angle'] < -45]
            center_obstacles = [obs for obs in front_obstacles if -45 <= obs['angle'] <= 45]
            right_obstacles = [obs for obs in front_obstacles if obs['angle'] > 45]
            
            # 计算各区域最近距离
            left_dist = min([obs['distance'] for obs in left_obstacles]) if left_obstacles else float('inf')
            center_dist = min([obs['distance'] for obs in center_obstacles]) if center_obstacles else float('inf')
            right_dist = min([obs['distance'] for obs in right_obstacles]) if right_obstacles else float('inf')
            
            print(f"📡 雷达检测: 左={left_dist:.1f}cm 中={center_dist:.1f}cm 右={right_dist:.1f}cm")
            
            # 紧急停车条件
            if center_dist < self.safe_distance:
                decision['action'] = 'stop'
                decision['speed'] = 0
                decision['reason'] = f'🚨 正前方障碍物距离 {center_dist:.1f}cm，紧急停车'
                
            # 减速条件
            elif center_dist < self.warning_distance:
                decision['speed'] = int(self.auto_speed * 0.3)
                decision['reason'] = f'⚠️ 前方障碍物距离 {center_dist:.1f}cm，减速行驶'
                
            # 转向躲避
            elif left_dist < right_dist and left_dist < self.warning_distance:
                decision['action'] = 'right'
                decision['speed'] = int(self.auto_speed * 0.5)
                decision['reason'] = f'↻ 左侧有障碍物({left_dist:.1f}cm)，向右躲避'
                
            elif right_dist < left_dist and right_dist < self.warning_distance:
                decision['action'] = 'left'
                decision['speed'] = int(self.auto_speed * 0.5)
                decision['reason'] = f'↺ 右侧有障碍物({right_dist:.1f}cm)，向左躲避'

    # 超声波辅助检测
    if ultrasonic_data and ultrasonic_data.get('min_distance', 0) > 0:
        us_min = ultrasonic_data['min_distance']
        print(f"🔊 超声波检测: {us_min}cm")
        
        if us_min < self.safe_distance * 0.8:
            decision['action'] = 'stop'
            decision['speed'] = 0
            decision['reason'] = f'🔊 超声波检测到障碍物距离 {us_min}cm，紧急停车'
        elif us_min < self.warning_distance:
            if decision['speed'] > self.auto_speed * 0.3:
                decision['speed'] = int(self.auto_speed * 0.3)
                decision['reason'] = f'🔊 超声波检测到障碍物，减速至 {decision["speed"]}'

    self.last_command = decision
    return decision
```

**参数**:
- `radar_obstacles`: 雷达障碍物列表
- `ultrasonic_data`: 超声波数据
- `pose_data`: 姿态数据

**返回**: 决策对象

**决策逻辑**:
1. **检测范围**: 前方270度（-135°到135°）
2. **区域划分**: 左侧（-135°到-45°）、中心（-45°到45°）、右侧（45°到135°）
3. **决策策略**:
   - 紧急停车: 中心区域障碍物距离 < 安全距离
   - 减速: 中心区域障碍物距离 < 警告距离
   - 转向: 侧面障碍物更近且距离 < 警告距离
   - 超声波优先: 超声波检测距离 < 安全距离×0.8时紧急停车

##### get_last_command - 获取最后命令

```python
def get_last_command(self):
    return self.last_command
```

**返回**: 最后执行的命令对象

##### get_mode_info - 获取模式信息

```python
def get_mode_info(self):
    return {
        'auto_mode': self.auto_mode,
        'auto_speed': self.auto_speed,
        'safe_distance': self.safe_distance,
        'warning_distance': self.warning_distance
    }
```

**返回**: 当前模式和参数信息

---

### 1.3 MQTT服务 - services/mqtt_service.py

**文件路径**: `services/mqtt_service.py`  
**文件大小**: ~4KB  
**功能描述**: MQTT消息收发服务，连接小车和云端

#### 1.3.1 类定义

```python
class MQTTService:
    def __init__(self, config, data_processor, decision_engine):
        self.config = config
        self.data_processor = data_processor
        self.decision_engine = decision_engine
        self.client = None
        self.connected = False
        self.on_message_callback = None
        self.loop = None
```

**属性说明**:
- `config`: 配置对象
- `data_processor`: 数据处理器
- `decision_engine`: 决策引擎
- `client`: MQTT客户端
- `connected`: 连接状态
- `on_message_callback`: 消息回调函数
- `loop`: 事件循环

#### 1.3.2 方法详解

##### set_event_loop - 设置事件循环

```python
def set_event_loop(self, loop):
    self.loop = loop
```

**参数**: `loop`: asyncio事件循环

##### on_connect - 连接回调

```python
def on_connect(self, client, userdata, flags, rc):
    if rc == 0:
        self.connected = True
        print(f"✅ MQTT连接成功")
        self.client.subscribe(self.config.MQTT_TOPIC)
        self.client.subscribe(self.config.CAMERA_TOPIC)
    else:
        print(f"❌ MQTT连接失败，返回码: {rc}")
```

**功能**: MQTT连接成功后订阅主题

##### on_disconnect - 断开回调

```python
def on_disconnect(self, client, userdata, rc):
    self.connected = False
    print("🔌 MQTT连接断开")
```

**功能**: MQTT连接断开时更新状态

##### on_message - 消息接收回调

```python
def on_message(self, client, userdata, msg):
    try:
        topic = msg.topic
        payload = msg.payload.decode('utf-8')
        
        print(f"\n📥 收到MQTT消息: {topic}")

        if topic == self.config.MQTT_TOPIC:
            data = json.loads(payload)
            self.process_sensor_data(data)
            
            if self.on_message_callback and self.loop:
                asyncio.run_coroutine_threadsafe(self.on_message_callback(data), self.loop)

        elif topic == self.config.CAMERA_TOPIC:
            if self.on_message_callback and self.loop:
                asyncio.run_coroutine_threadsafe(self.on_message_callback({'type': 'camera', 'data': payload}), self.loop)

    except Exception as e:
        print(f"❌ MQTT消息处理错误: {e}")
```

**功能**: 处理接收到的MQTT消息

##### process_sensor_data - 处理传感器数据

```python
def process_sensor_data(self, data):
    if 'radar' in data:
        print(f"📡 雷达点数: {len(data['radar'])}")
        self.data_processor.process_radar_data(data['radar'])
        
    if 'ultrasonic' in data:
        print(f"🔊 超声波数据: {data['ultrasonic']}")
        self.data_processor.process_ultrasonic_data(data['ultrasonic'])
        
    if 'pose' in data:
        self.data_processor.process_pose_data(data['pose'])

    # 自动模式下进行决策
    if self.decision_engine.auto_mode:
        latest = self.data_processor.get_latest_data()
        radar_data = latest['radar']['data'] if (latest['radar'] and latest['radar'].get('data')) else []
        ultrasonic_data = latest['ultrasonic']['data'] if (latest['ultrasonic'] and latest['ultrasonic'].get('data')) else {}
        
        decision = self.decision_engine.get_decision(radar_data, ultrasonic_data, {})
        self.publish_command(decision)
```

**功能**: 处理传感器数据并在自动模式下生成决策

##### publish_command - 发布控制命令

```python
def publish_command(self, command):
    if self.connected and self.client:
        try:
            payload = json.dumps(command)
            self.client.publish(self.config.MQTT_COMMAND_TOPIC, payload)
            print(f"📤 发布命令: {command['action']} ({command['speed']}) - {command['reason']}")
        except Exception as e:
            print(f"❌ MQTT发布失败: {e}")
```

**功能**: 向小车发送控制命令

##### start - 启动MQTT服务

```python
def start(self):
    self.client = mqtt.Client()
    self.client.on_connect = self.on_connect
    self.client.on_disconnect = self.on_disconnect
    self.client.on_message = self.on_message
    
    try:
        self.client.connect(self.config.MQTT_BROKER, self.config.MQTT_PORT, 60)
        self.client.loop_start()
        print("🚀 MQTT服务已启动")
    except Exception as e:
        print(f"❌ MQTT连接异常: {e}")
```

**功能**: 初始化并启动MQTT客户端

##### stop - 停止MQTT服务

```python
def stop(self):
    if self.client:
        self.client.loop_stop()
        self.client.disconnect()
        print("🛑 MQTT服务已停止")
```

**功能**: 停止MQTT客户端

---

### 1.4 数据处理服务 - services/data_processor.py

**文件路径**: `services/data_processor.py`  
**文件大小**: ~2KB  
**功能描述**: 处理和存储传感器数据

#### 1.4.1 类定义

```python
class DataProcessor:
    def __init__(self):
        self.radar_data = {'timestamp': None, 'data': None}
        self.ultrasonic_data = {'timestamp': None, 'data': None}
        self.pose_data = {'timestamp': None, 'data': None}
```

#### 1.4.2 方法详解

##### process_radar_data - 处理雷达数据

```python
def process_radar_data(self, radar_data):
    self.radar_data = {
        'timestamp': datetime.now().isoformat(),
        'data': radar_data
    }
```

##### process_ultrasonic_data - 处理超声波数据

```python
def process_ultrasonic_data(self, ultrasonic_data):
    self.ultrasonic_data = {
        'timestamp': datetime.now().isoformat(),
        'data': ultrasonic_data
    }
```

##### process_pose_data - 处理姿态数据

```python
def process_pose_data(self, pose_data):
    self.pose_data = {
        'timestamp': datetime.now().isoformat(),
        'data': pose_data
    }
```

##### get_latest_data - 获取最新数据

```python
def get_latest_data(self):
    return {
        'radar': self.radar_data,
        'ultrasonic': self.ultrasonic_data,
        'pose': self.pose_data
    }
```

---

### 1.5 配置文件 - config.py

**文件路径**: `config.py`  
**文件大小**: ~1KB  
**功能描述**: 系统配置参数

```python
import os

class Config:
    # API配置
    API_HOST = os.getenv('API_HOST', '0.0.0.0')
    API_PORT = int(os.getenv('API_PORT', 8000))
    
    # MQTT配置
    MQTT_BROKER = os.getenv('MQTT_BROKER', 'localhost')
    MQTT_PORT = int(os.getenv('MQTT_PORT', 1883))
    MQTT_TOPIC = 'car/sensors'
    MQTT_COMMAND_TOPIC = 'car/command'
    CAMERA_TOPIC = 'car/camera'
    
    # 安全配置
    CONTROL_PASSWORD = os.getenv('CONTROL_PASSWORD', '123456')
```

**配置项说明**:
- `API_HOST`: API服务监听地址
- `API_PORT`: API服务端口
- `MQTT_BROKER`: MQTT代理地址
- `MQTT_PORT`: MQTT端口
- `MQTT_TOPIC`: 传感器数据主题
- `MQTT_COMMAND_TOPIC`: 控制命令主题
- `CAMERA_TOPIC`: 摄像头数据主题
- `CONTROL_PASSWORD`: 控制密码

---

### 1.6 依赖文件 - requirements.txt

**文件路径**: `requirements.txt`  
**文件大小**: ~200B  
**功能描述**: Python依赖包列表

```
fastapi==0.104.1
uvicorn==0.24.0
paho-mqtt==1.6.1
python-multipart==0.0.6
```

**依赖包说明**:
- `fastapi`: 现代Web框架
- `uvicorn`: ASGI服务器
- `paho-mqtt`: MQTT客户端库
- `python-multipart`: 文件上传支持

---

### 1.7 Web前端 - static/index.html

**文件路径**: `static/index.html`  
**文件大小**: ~15KB  
**功能描述**: Web监控和控制界面

#### 1.7.1 主要功能模块

##### 登录模块
- 密码输入框
- 登录按钮
- 错误提示

##### 摄像头显示
- 实时视频流
- Base64图像解码
- 占位符显示

##### 雷达点阵图
- 200x200像素圆形雷达
- 4个同心圆
- 障碍物点显示
- 动态更新

##### 超声波显示
- 距离进度条
- 最小距离数值
- 颜色渐变

##### 姿态数据显示
- 加速度计X/Y/Z
- 陀螺仪X/Y/Z
- 实时数值更新

##### 控制面板
- 模式切换（手动/自动）
- 速度滑块
- 方向控制按钮
- 自动避障参数设置

##### 状态显示
- 连接状态指示
- 运动状态
- 最后命令
- 决策原因

#### 1.7.2 JavaScript功能

##### WebSocket连接
```javascript
const ws = new WebSocket('ws://' + window.location.host + '/ws');
ws.onmessage = function(event) {
    const data = JSON.parse(event.data);
    updateUI(data);
};
```

##### API调用
```javascript
async function sendCommand(action, speed) {
    const response = await fetch('/api/command', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({action, speed})
    });
    return await response.json();
}
```

##### 雷达点绘制
```javascript
function drawRadarPoints(radarData) {
    const container = document.getElementById('radarPoints');
    container.innerHTML = '';
    
    radarData.forEach(point => {
        const div = document.createElement('div');
        div.className = 'radar-point';
        const x = 100 + point.distance * Math.cos(point.angle * Math.PI / 180) * 0.8;
        const y = 100 + point.distance * Math.sin(point.angle * Math.PI / 180) * 0.8;
        div.style.left = x + 'px';
        div.style.top = y + 'px';
        container.appendChild(div);
    });
}
```

---

## 二、API接口文档

### 2.1 REST API

#### 2.1.1 健康检查

**接口**: `GET /health`  
**描述**: 检查服务健康状态

**请求示例**:
```bash
curl http://localhost:8000/health
```

**响应示例**:
```json
{
    "status": "healthy",
    "mqtt_connected": true
}
```

#### 2.1.2 获取最新数据

**接口**: `GET /latest`  
**描述**: 获取最新的传感器数据和命令

**请求示例**:
```bash
curl http://localhost:8000/latest
```

**响应示例**:
```json
{
    "timestamp": "2026-05-12T10:30:00.000Z",
    "radar": {
        "timestamp": "2026-05-12T10:30:00.000Z",
        "data": [{"angle": 0, "distance": 60}]
    },
    "ultrasonic": {
        "timestamp": "2026-05-12T10:30:00.000Z",
        "data": {"front": 55, "min_distance": 55}
    },
    "pose": {
        "timestamp": "2026-05-12T10:30:00.000Z",
        "data": {"acc_x": 0.1, "acc_y": 0.02, "acc_z": 9.8}
    },
    "last_command": {
        "action": "forward",
        "speed": 20,
        "timestamp": "2026-05-12T10:30:00.500Z",
        "reason": "安全"
    },
    "mode_info": {
        "auto_mode": true,
        "auto_speed": 20,
        "safe_distance": 50,
        "warning_distance": 100
    },
    "camera": "base64_encoded_image"
}
```

#### 2.1.3 密码认证

**接口**: `POST /api/login`  
**描述**: 客户端密码认证

**请求示例**:
```bash
curl -X POST http://localhost:8000/api/login \
  -H "Content-Type: application/json" \
  -d '{"password": "123456"}'
```

**响应示例**:
```json
{
    "success": true,
    "message": "认证成功"
}
```

#### 2.1.4 发送控制命令

**接口**: `POST /api/command`  
**描述**: 发送手动控制命令或切换自动模式

**请求示例（手动控制）**:
```bash
curl -X POST http://localhost:8000/api/command \
  -H "Content-Type: application/json" \
  -d '{"action": "forward", "speed": 30}'
```

**请求示例（自动模式）**:
```bash
curl -X POST http://localhost:8000/api/command \
  -H "Content-Type: application/json" \
  -d '{
    "auto_mode": true,
    "speed": 20,
    "safe_distance": 50,
    "warning_distance": 100
  }'
```

**响应示例**:
```json
{
    "success": true,
    "message": "命令已发送: forward",
    "action": "forward",
    "speed": 30
}
```

#### 2.1.5 获取当前模式

**接口**: `GET /api/mode`  
**描述**: 获取当前控制模式和参数

**请求示例**:
```bash
curl http://localhost:8000/api/mode
```

**响应示例**:
```json
{
    "auto_mode": true,
    "auto_speed": 20,
    "safe_distance": 50,
    "warning_distance": 100
}
```

### 2.2 WebSocket API

#### 2.2.1 实时数据推送

**接口**: `WS /ws`  
**描述**: 实时推送传感器数据和控制命令

**连接示例**:
```javascript
const ws = new WebSocket('ws://localhost:8000/ws');
ws.onmessage = function(event) {
    const data = JSON.parse(event.data);
    console.log(data);
};
```

**推送数据格式**:
```json
{
    "type": "sensor_data",
    "timestamp": "2026-05-12T10:30:00.000Z",
    "radar": {
        "timestamp": "2026-05-12T10:30:00.000Z",
        "data": [...]
    },
    "ultrasonic": {
        "timestamp": "2026-05-12T10:30:00.000Z",
        "data": {...}
    },
    "pose": {
        "timestamp": "2026-05-12T10:30:00.000Z",
        "data": {...}
    },
    "last_command": {
        "action": "forward",
        "speed": 20,
        "timestamp": "2026-05-12T10:30:00.500Z",
        "reason": "安全"
    },
    "mode_info": {
        "auto_mode": true,
        "auto_speed": 20,
        "safe_distance": 50,
        "warning_distance": 100
    },
    "camera": "base64_encoded_image"
}
```

---

## 三、数据协议

### 3.1 小车端发送数据

#### 3.1.1 传感器数据

**主题**: `car/sensors`  
**频率**: 10-20Hz  
**格式**: JSON

```json
{
    "radar": [
        {"angle": -45, "distance": 120},
        {"angle": -30, "distance": 85},
        {"angle": 0, "distance": 60},
        {"angle": 30, "distance": 95},
        {"angle": 45, "distance": 110}
    ],
    "ultrasonic": {
        "front": 55,
        "left": 120,
        "right": 80,
        "back": 200,
        "min_distance": 55
    },
    "pose": {
        "acc_x": 0.15,
        "acc_y": 0.02,
        "acc_z": 9.78,
        "gyro_x": 0.01,
        "gyro_y": 0.005,
        "gyro_z": 0.02
    },
    "timestamp": "2026-05-12T10:30:00.000Z"
}
```

**字段说明**:
- `radar`: 雷达点云数据
  - `angle`: 角度（度，-180到180）
  - `distance`: 距离（厘米）
- `ultrasonic`: 超声波数据
  - `front`: 前方距离
  - `left`: 左侧距离
  - `right`: 右侧距离
  - `back`: 后方距离
  - `min_distance`: 最小距离
- `pose`: 姿态数据
  - `acc_x/y/z`: 加速度（g）
  - `gyro_x/y/z`: 角速度（度/秒）
- `timestamp`: 时间戳（ISO 8601格式）

#### 3.1.2 摄像头数据

**主题**: `car/camera`  
**频率**: 10-30fps  
**格式**: Base64编码的JPEG图像

```
/9j/4AAQSkZJRgABAQAAAQABAAD/2wBD...
```

### 3.2 云端返回数据

#### 3.2.1 控制命令

**主题**: `car/command`  
**频率**: 实时  
**格式**: JSON

```json
{
    "action": "forward",
    "speed": 20,
    "timestamp": "2026-05-12T10:30:00.500Z",
    "reason": "安全"
}
```

**动作类型**:
- `forward`: 前进
- `backward`: 后退
- `left`: 左转
- `right`: 右转
- `stop`: 停止

**字段说明**:
- `action`: 动作类型
- `speed`: 速度值（0-100）
- `timestamp`: 时间戳
- `reason`: 决策原因

---

## 四、系统流程

### 4.1 系统启动流程

```
1. 启动FastAPI应用
   ↓
2. 创建服务实例
   - Config
   - DataProcessor
   - DecisionEngine
   - MQTTService
   ↓
3. 触发startup事件
   ↓
4. 启动MQTT服务
   - 连接MQTT Broker
   - 订阅主题
   ↓
5. 设置事件循环
   ↓
6. 注册消息回调
   ↓
7. 启动Uvicorn服务器
   ↓
8. 系统就绪
```

### 4.2 传感器数据处理流程

```
1. 小车发送传感器数据
   ↓
2. MQTT接收消息
   ↓
3. on_message回调触发
   ↓
4. 解析JSON数据
   ↓
5. 分发到DataProcessor
   - process_radar_data()
   - process_ultrasonic_data()
   - process_pose_data()
   ↓
6. 存储最新数据
   ↓
7. 如果是自动模式
   ↓
8. DecisionEngine.get_decision()
   - 分析雷达数据
   - 分析超声波数据
   - 生成决策
   ↓
9. 发布控制命令
   ↓
10. 广播到WebSocket连接
```

### 4.3 手动控制流程

```
1. 用户在Web界面点击控制按钮
   ↓
2. 前端发送POST请求到 /api/command
   ↓
3. 验证客户端认证状态
   ↓
4. 解析命令参数
   ↓
5. 更新latest_manual_command
   ↓
6. 发布到MQTT
   ↓
7. 小车接收并执行命令
   ↓
8. 返回响应给前端
```

### 4.4 自动避障流程

```
1. 用户切换到自动模式
   ↓
2. DecisionEngine.set_auto_mode(true)
   ↓
3. 接收传感器数据
   ↓
4. 分析前方障碍物
   - 检测270度范围
   - 分区（左/中/右）
   - 计算最近距离
   ↓
5. 应用决策逻辑
   - 紧急停车: < safe_distance
   - 减速: < warning_distance
   - 转向: 侧面障碍物更近
   ↓
6. 超声波辅助检测
   - 更高优先级
   - 紧急停车: < safe_distance * 0.8
   ↓
7. 生成控制命令
   ↓
8. 发布到MQTT
   ↓
9. 小车执行避障动作
```

---

## 五、部署配置

### 5.1 环境变量

创建 `.env` 文件:

```env
API_HOST=0.0.0.0
API_PORT=8000
MQTT_BROKER=localhost
MQTT_PORT=1883
CONTROL_PASSWORD=your_secure_password
```

### 5.2 启动命令

#### 开发环境

```bash
python app.py
```

#### 生产环境

```bash
nohup python app.py > logs/app.log 2>&1 &
```

#### 使用systemd

创建 `/etc/systemd/system/car-cloud.service`:

```ini
[Unit]
Description=Car Cloud AI Service
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/car-cloud-service
Environment="PATH=/path/to/car-cloud-service/venv/bin"
ExecStart=/path/to/car-cloud-service/venv/bin/python app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务:

```bash
sudo systemctl daemon-reload
sudo systemctl enable car-cloud
sudo systemctl start car-cloud
```

---

## 六、故障排查

### 6.1 常见问题

#### 问题1: MQTT连接失败

**症状**: 日志显示 "MQTT连接失败"

**解决方案**:
1. 检查MQTT Broker是否运行
2. 检查网络连接
3. 检查端口是否被占用
4. 检查防火墙设置

#### 问题2: WebSocket连接断开

**症状**: Web界面频繁断开

**解决方案**:
1. 检查网络稳定性
2. 增加心跳间隔
3. 检查服务器负载
4. 查看浏览器控制台错误

#### 问题3: 自动避障不灵敏

**症状**: 小车未能及时避障

**解决方案**:
1. 调整 `safe_distance` 参数
2. 调整 `warning_distance` 参数
3. 检查传感器数据质量
4. 增加传感器采样频率

### 6.2 日志分析

#### 应用日志

```bash
tail -f logs/app.log
```

#### MQTT日志

```bash
sudo journalctl -u mosquitto -f
```

#### 系统日志

```bash
sudo journalctl -u car-cloud -f
```

---

## 七、性能优化

### 7.1 代码优化

- 使用异步IO提高并发性能
- 减少不必要的计算
- 优化数据结构
- 使用缓存减少重复计算

### 7.2 网络优化

- 使用CDN加速静态资源
- 启用HTTP/2
- 压缩响应数据
- 优化WebSocket消息频率

### 7.3 资源优化

- 限制WebSocket连接数
- 定期清理历史数据
- 使用连接池
- 优化内存使用

---

## 八、安全建议

### 8.1 密码安全

- 使用强密码
- 定期更换密码
- 不要硬编码密码
- 使用环境变量存储密码

### 8.2 网络安全

- 使用HTTPS
- 启用MQTT认证
- 限制访问IP
- 配置防火墙规则

### 8.3 数据安全

- 加密敏感数据
- 定期备份数据
- 访问日志审计
- 异常行为监控

---

## 九、扩展功能

### 9.1 数据存储

- 集成InfluxDB存储时序数据
- 集成Redis缓存热点数据
- 数据持久化到数据库

### 9.2 数据分析

- 添加数据分析模块
- 生成统计报表
- 可视化数据展示
- 历史数据回放

### 9.3 机器学习

- 训练避障模型
- 预测障碍物行为
- 优化决策算法
- 自适应参数调整

---

## 十、总结

本项目是一个完整的云端AI避障系统，包含以下核心功能:

1. **实时数据接收**: 通过MQTT接收小车传感器数据
2. **智能决策**: 基于雷达和超声波数据进行避障决策
3. **Web监控**: 提供实时监控和控制界面
4. **手动/自动模式**: 支持手动控制和自动避障两种模式
5. **密码认证**: 保护控制接口安全

系统架构清晰，代码模块化，易于扩展和维护。

---

**文档版本**: v1.0  
**更新日期**: 2026-05-12  
**维护人员**: [王卓然]
